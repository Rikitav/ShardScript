# ============================================================================
# Capiparity configuration
# ----------------------------------------------------------------------------
# When capiparity is launched from a directory containing this file, the
# settings below are applied. Command-line arguments ALWAYS take priority
# over these values; config values take priority over built-in defaults.
# A missing key simply falls back to its default.
# ============================================================================

threshold = 100.0
format = "table"

api_header_prefix = ["D:/repos/Rikitav/ShardScript/ShardScript/include"]
flat_api_header = "D:/repos/Rikitav/ShardScript/ShardScript/include/shard/ShardScriptExtern.hpp"
#input = "ShardScript/include/shard/ShardScriptExtern.hpp.old"

compile_flags = [
    "-x", "c++",
    "-std=c++20",
    "-ID:/repos/Rikitav/ShardScript/ShardScript/include",
    "-isystem", "D:/repos/Rikitav/ShardScript/ShardScript/third_party/libuv/include",
    "-isystem", "D:/repos/Rikitav/ShardScript/ShardScript/third_party/mimalloc/include",
    "-DSHARD_API_EXPORT",
]

using stdio;

namespace typed_throw_catch_exact;

public class Program
{
    public static func Thrower() -> void
    {
        throw new RuntimeException();
    }

    public static func Main() -> void
    {
        try
        {
            Thrower();
        }
        catch (ex: RuntimeException)
        {
            println("caught");
        }
    }
}

using stdio;
using collections;

namespace Test;

public static func First<T>(source: IEnumerable<T>) -> T
{
	foreach (item in source)
	{
		return item;
	}
	return 0;
}

public static func Main() -> void
{
	test := [1..10];
	x := First<int>(test);
	println(x);
}

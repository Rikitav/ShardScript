using stdio;
using net;

public static func Main() -> void
{
    defer client := new HttpClient("http://httpbin.org");
    response := client.Get("/todos/1");

    println("Status received: " + response.StatusCode);
    if (response.StatusCode == 200)
    {
        println(response.Body);
    }
}
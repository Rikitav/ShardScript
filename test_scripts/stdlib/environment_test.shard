using stdio;
using environment;

namespace env_test;

public static func Main() -> void
{
    println(Environment.PROCESSOR_ARCHITECTURE);
    println(Environment.COMPUTERNAME);
    println(Environment.USERNAME);
    println(Environment.NONEXISTENT_VAR);
}

using stdio;
using async;

namespace test_generic_static_inference3;

public class Program
{
    public static func Main() -> void
    {
        task : ValueTask<string> = ValueTask.FromResult("hi");
        ValueTask.Wait(task);
        println(task.Result);
    }
}

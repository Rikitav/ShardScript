using stdio;

namespace test_try_catch_simple;

public class Program
{
    public static func Main() -> void
    {
        try
        {
            throw new RuntimeException();
        }
        catch (ex: RuntimeException)
        {
            println("caught");
        }
        println("done");
    }
}

using stdio;
using async;

namespace test_async_inf_any;

public class Program
{
    public static func Main() -> void
    {
        task := DoItAsync();
        Task.Wait(task);
    }

    public static async func DoItAsync() -> Task
    {
        i := 0;
        println(i + 1);
    }
}

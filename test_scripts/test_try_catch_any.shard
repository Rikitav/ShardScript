using stdio;

namespace test_try_catch_any;

public class Program
{
    public static func Main() -> void
    {
        try
        {
            throw new RuntimeException();
        }
        catch { ex: any }
        {
            println("caught");
        }
        println("done");
    }
}

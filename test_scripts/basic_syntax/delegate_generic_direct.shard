using stdio;

namespace Test;

public delegate Transform<T, U>(value: T) -> U;

public static func Make<T, U>(action: Transform<T, U>) -> Transform<T, U>
{
	return action;
}

public static func Main() -> void
{
	f := Make<int, int>(lambda (a: int) -> int
	{
		return a + 10;
	});

	println(f(5));
}

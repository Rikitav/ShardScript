using stdio;
using environment;

namespace script_args_marker;

public class Program
{
    public static func Main() -> void
    {
        args := Environment.Args;
        println(args.Length);
        foreach (arg in args)
        {
            println(arg);
        }
    }
}

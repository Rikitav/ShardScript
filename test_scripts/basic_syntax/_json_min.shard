using stdio;
using json;

namespace json_min;

public static func Main() -> void
{
    node: JsonNode = JsonNode.Parse("{\"a\":1,\"b\":\"hi\"}");
    print("kind="); println(node.Kind());
    print("a="); println(node.Get("a").AsInt());
    print("b="); println(node.Get("b").AsString());
}
